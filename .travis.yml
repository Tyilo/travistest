matrix:
  include:
    - os: linux
      dist: trusty
      sudo: required
      compiler: gcc5
      addons:
        apt:
          packages:
            - gcc-5
            - g++-5
    - os: linux
      dist: trusty
      sudo: required
      compiler: clang
      addons:
        apt:
          packages:
            - gcc-5
            - g++-5
    - os: osx
      sudo: false
      compiler: clang
language: cpp
script:
  - uname -a
  - echo /usr/bin/g++-*
  - c++ --version
  - echo $CC
  - echo $CXX
  - c++ foo.cpp -std=c++11
